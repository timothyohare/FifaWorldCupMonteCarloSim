{
  "name": "wc-montecarlo-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "FIFA World Cup 2026 Monte Carlo simulator — current standings to title odds.",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:acceptance": "vitest run src/acceptance.test.ts",
    "report": "tsx spikes/report.ts",
    "sim": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0"
  }
}
