{
  "name": "wc-montecarlo-spikes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Throwaway exploratory spikes for the World Cup Monte Carlo simulator (planning stage).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "report": "tsx spikes/report.ts",
    "sim": "tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
