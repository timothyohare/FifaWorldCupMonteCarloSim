node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example

# Fetched / derived data artifacts — reproducible via scripts + data/README.md, not committed
# (the official PDF, extracted text, raw ESPN dumps, the 3.6MB results CSV…)
data/*
!data/README.md
